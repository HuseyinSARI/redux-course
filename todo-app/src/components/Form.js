import React from 'react'

function Form() {
    return (
        <form>
            <input className='new-todo' placeholder='what needs to be done?' autoFocus />
        </form>
    )
}

export default Form